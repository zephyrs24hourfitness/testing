export class Hotbar{constructor(blocks){this.blocks=blocks;this.selected=0;this.el=document.createElement('div');this.el.className='hotbar';document.body.appendChild(this.el);addEventListener('keydown',e=>{const n=Number(e.key);if(n>=1&&n<=8)this.selected=n-1;});this.render();} render(){this.el.innerHTML=this.blocks.slice(0,8).map((b,i)=>`<div class='slot ${i===this.selected?'sel':''}'>${i+1}<br>${b}</div>`).join('');} tick(){this.render();}}
