import {BLOCK} from './BlockRegistry';import {Noise} from '../utils/Noise';import {CaveGenerator} from './CaveGenerator';
export class TerrainGenerator{constructor(seed,worldHeight,seaLevel){this.noise=new Noise(seed);this.cave=new CaveGenerator(this.noise);this.worldHeight=worldHeight;this.seaLevel=seaLevel;} heightAt(x,z){const m=this.noise.ridge(x*0.003,z*0.003,4)*36;const h=this.noise.fbm2(x*0.006,z*0.006,5)*10;const p=this.noise.fbm2(x*0.02,z*0.02,3)*4;return Math.floor(38+m+h+p);} blockAt(x,y,z){const h=this.heightAt(x,z);if(y===0)return BLOCK.BEDROCK;if(y>h)return y<=this.seaLevel?BLOCK.WATER:BLOCK.AIR; if(this.cave.carve(x,y,z,h)) return BLOCK.AIR; if(y===h){if(h<=this.seaLevel+1)return BLOCK.SAND;return BLOCK.GRASS;} if(y>h-4)return BLOCK.DIRT; if(this.noise.value3(x*0.1,y*0.1,z*0.1)>0.72) return BLOCK.COAL; return BLOCK.STONE;}}
