export class Input{constructor(dom){this.keys={};this.mouse={dx:0,dy:0};this.locked=false;dom.addEventListener('click',()=>dom.requestPointerLock());document.addEventListener('pointerlockchange',()=>this.locked=document.pointerLockElement===dom);addEventListener('keydown',e=>this.keys[e.code]=true);addEventListener('keyup',e=>this.keys[e.code]=false);addEventListener('mousemove',e=>{if(this.locked){this.mouse.dx+=e.movementX;this.mouse.dy+=e.movementY;}});} consumeMouse(){const m={...this.mouse};this.mouse.dx=this.mouse.dy=0;return m;}}
